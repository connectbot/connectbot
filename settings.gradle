pluginManagement {
	repositories {
		google()
		gradlePluginPortal()
	}
}

ext.TRANSLATIONS_ONLY = System.getenv("TRANSLATIONS_ONLY")?.trim()

if (!TRANSLATIONS_ONLY) {
	include ':app'
}
include ':translations'
